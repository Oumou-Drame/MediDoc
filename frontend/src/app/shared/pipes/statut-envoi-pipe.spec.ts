import { StatutEnvoiPipe } from './statut-envoi-pipe';

describe('StatutEnvoiPipe', () => {
  it('create an instance', () => {
    const pipe = new StatutEnvoiPipe();
    expect(pipe).toBeTruthy();
  });
});
